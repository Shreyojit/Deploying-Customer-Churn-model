# Deploying Customer Churn Model

This project demonstrates how to deploy a machine learning model for customer churn prediction using MLflow and DVC.

## Installation

To install the project, run:

```bash
pip install -e .
